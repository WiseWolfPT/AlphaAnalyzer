# Production Environment Variables Template
# =================================================================================================
# PRODUCTION DEPLOYMENT TEMPLATE - Fill with real values for production
# Copy this to .env.prod when deploying to production
# =================================================================================================

# Supabase Configuration (Production)
# Get these from your Supabase project dashboard → Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Upstash Redis Configuration (Rate Limiting)
# Get these from https://upstash.com dashboard → Redis → your-database
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here

# Financial Data API Keys (Production)
# Replace with your production API keys from respective providers
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_production_key
TWELVE_DATA_API_KEY=your_twelve_data_production_key
FMP_API_KEY=your_fmp_production_key
FINNHUB_API_KEY=your_finnhub_production_key

# Payment Processing (Production)
# Get these from Stripe Dashboard → Developers → API Keys
STRIPE_SECRET_KEY=sk_live_your_stripe_live_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_live_webhook_secret

# Authentication & Security
# Generate a strong 32+ character random string for JWT signing
JWT_SECRET=your_super_secure_production_jwt_secret_minimum_32_characters

# Server Configuration
PORT=3001
NODE_ENV=production

# Optional: Additional Services (Production)
# CHATGPT_API_KEY=your_openai_production_api_key
# POLYGON_API_KEY=your_polygon_production_api_key

# Optional: Email Service (Production)
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASS=your_sendgrid_api_key

# Optional: Error Tracking (Production)
# SENTRY_DSN=your_production_sentry_dsn

# Cache Configuration (Production)
MAX_CACHE_SIZE_MB=1024

# Monitoring and Logging (Production)
LOG_LEVEL=info

# =================================================================================================
# FRONTEND VARIABLES (PRODUCTION)
# These should be set in your Vercel/deployment platform environment variables:
# 
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# VITE_API_BASE_URL=https://your-domain.vercel.app
# =================================================================================================

# =================================================================================================
# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this file: cp .env.prod.example .env.prod
# 2. Fill in all production values
# 3. Use platform-specific environment variable settings (Vercel, Railway, etc.)
# 4. NEVER commit .env.prod to version control
# =================================================================================================