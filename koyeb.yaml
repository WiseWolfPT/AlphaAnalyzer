app:
  name: alphaanalyzer-backend
  
build:
  type: dockerfile
  dockerfile: Dockerfile.koyeb
  
env:
  - key: NODE_ENV
    value: production
  # Remove PORT from here, let Koyeb assign it
  
service:
  ports:
    - port: 3001
      protocol: http
  
  routes:
    - path: /
      port: 3001
  
  healthcheck:
    path: /api/health
    interval: 30
    timeout: 10
    
  resources:
    instance_type: free
    
  regions:
    - fra # Frankfurt
    
  autoscaling:
    min: 1
    max: 1