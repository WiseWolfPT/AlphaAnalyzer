# =================================================================================================
# FRONTEND ENVIRONMENT VARIABLES (ROADMAP V4)
# =================================================================================================
# These variables are safe to expose to the client-side code
# They will be included in the frontend bundle and visible to users
# NEVER put sensitive data here (API keys, secrets, etc.)
# =================================================================================================

# Supabase Frontend Configuration
# These are safe to expose - anon key is designed for client-side use
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API Configuration  
# Point to your backend server
VITE_API_BASE_URL=http://localhost:3001/api

# Optional: Analytics and Monitoring
# VITE_GOOGLE_ANALYTICS_ID=GA_TRACKING_ID
# VITE_POSTHOG_KEY=your_posthog_key_here

# Optional: Feature Flags
# VITE_ENABLE_TRANSCRIPTS=true
# VITE_ENABLE_PORTFOLIOS=true
# VITE_ENABLE_ADVANCED_CHARTS=true

# =================================================================================================
# HOW TO USE THIS FILE:
# 1. Copy this file: cp .env.public.example .env.public
# 2. Fill in the values with your actual frontend configuration
# 3. Variables with VITE_ prefix will be available in your React components
# 4. These values will be visible to users, so keep them safe and non-sensitive
# =================================================================================================