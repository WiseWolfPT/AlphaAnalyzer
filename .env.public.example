############################################################################
# WARNING: PUBLIC ENVIRONMENT VARIABLES ONLY                               #
#                                                                          #
# ALL VARIABLES IN THIS FILE ARE EXPOSED IN THE CLIENT-SIDE JAVASCRIPT    #
# DO NOT PUT ANY SECRETS, PRIVATE KEYS, OR SENSITIVE DATA HERE           #
#                                                                          #
# Only use VITE_ prefixed variables for data that is safe to be public   #
# All server-side secrets should go in .env (which should never be shared)#
############################################################################

# Supabase Configuration (Public Keys Only)
# These are client-side keys that are meant to be public
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Application URL
# Used for generating absolute URLs in the client
# VITE_APP_URL=http://localhost:5173

# REMINDER: 
# - These variables will be bundled into your JavaScript code
# - They are visible to anyone who views your website source
# - Never put API secrets, database passwords, or private keys here
# - For server-side secrets, use the .env file instead